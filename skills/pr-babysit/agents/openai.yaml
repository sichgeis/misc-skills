interface:
  display_name: "PR Babysit"
  short_description: "Watch and clean PRs for human review"
  default_prompt: "Use $pr-babysit to create and watch a PR, safely clean settled bot chatter after it is ready, and return a copy-ready human-review handoff."
