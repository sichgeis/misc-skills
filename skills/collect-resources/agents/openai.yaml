interface:
  display_name: "Collect Resources"
  short_description: "Build a new-thread resource ingestion prompt"
  default_prompt: "Use $collect-resources to create a starter prompt for a new coding-agent thread that reads the most relevant resources and prepares for the task ahead."
