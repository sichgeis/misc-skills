interface:
  display_name: "Collect Resources"
  short_description: "Build a new-conversation resource prompt"
  default_prompt: "Use $collect-resources to collect the most relevant resources and, when supported, start a new user-visible conversation that reads and prepares for the task ahead."
